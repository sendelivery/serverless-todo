import { style } from "@vanilla-extract/css";

const table = style({
  display: "flex",
  flexDirection: "column",
});

const headings = style({});

const items = style({});

export default { table, headings, items };
